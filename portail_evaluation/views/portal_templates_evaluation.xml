<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Carte « Mes évaluations » sur l'accueil du portail -->
    <template id="portal_my_home_evaluation" name="Portail : carte Évaluations"
              inherit_id="portal.portal_my_home" priority="50">
        <xpath expr="//div[hasclass('o_portal_docs')]" position="inside">
            <t t-call="portal.portal_docs_entry">
                <t t-set="title">Mes évaluations</t>
                <t t-set="url" t-value="'/my/evaluations'"/>
                <t t-set="text">Consulter mon évaluation annuelle et m'auto-évaluer</t>
                <t t-set="config_card" t-value="True"/>
                <t t-set="placeholder_count" t-value="'appraisal_count'"/>
            </t>
            <t t-if="portail_is_evaluateur" t-call="portal.portal_docs_entry">
                <t t-set="title">Évaluations de mon équipe</t>
                <t t-set="url" t-value="'/my/team/evaluations'"/>
                <t t-set="text">Noter mes collaborateurs et suivre leur entretien</t>
                <t t-set="config_card" t-value="True"/>
                <t t-set="placeholder_count" t-value="'team_appraisal_count'"/>
            </t>
            <t t-call="portal.portal_docs_entry">
                <t t-set="title">Mes objectifs</t>
                <t t-set="url" t-value="'/my/mes-objectifs'"/>
                <t t-set="text">Voir les objectifs de mon exercice et déclarer mon avancement</t>
                <t t-set="config_card" t-value="True"/>
                <t t-set="placeholder_count" t-value="'mes_objectifs_count'"/>
            </t>
            <t t-if="portail_has_objectifs" t-call="portal.portal_docs_entry">
                <t t-set="title">Objectifs de mes collaborateurs</t>
                <t t-set="url" t-value="'/my/objectifs'"/>
                <t t-set="text">Fixer les objectifs de l'exercice, en début d'année</t>
                <t t-set="config_card" t-value="True"/>
                <t t-set="placeholder_count" t-value="'objectifs_count'"/>
            </t>
        </xpath>
    </template>

    <template id="portal_breadcrumbs_evaluation" name="Portail : fil d'ariane évaluations"
              inherit_id="portal.portal_breadcrumbs" priority="50">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'appraisal'" class="breadcrumb-item">
                <a t-if="appraisal" href="/my/evaluations">Mes évaluations</a>
                <t t-else="">Mes évaluations</t>
            </li>
            <li t-if="page_name == 'appraisal' and appraisal" class="breadcrumb-item active">
                <t t-esc="appraisal.ev_campagne_id.name or 'Évaluation'"/>
            </li>
            <li t-if="page_name == 'team_appraisal'" class="breadcrumb-item">
                <a t-if="appraisal" href="/my/team/evaluations">Évaluations de mon équipe</a>
                <t t-else="">Évaluations de mon équipe</t>
            </li>
            <li t-if="page_name == 'team_appraisal' and appraisal" class="breadcrumb-item active">
                <t t-esc="appraisal.employee_id.name"/>
            </li>
            <li t-if="page_name == 'objectifs'" class="breadcrumb-item">
                <a href="/my/objectifs">Objectifs</a>
            </li>
            <li t-if="page_name == 'mes_objectifs'" class="breadcrumb-item active">
                Mes objectifs
            </li>
        </xpath>
    </template>

    <!-- ==================== Mes objectifs ==================== -->
    <template id="portal_mes_objectifs_agent" name="Mes objectifs">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>
            <t t-call="portal.portal_searchbar">
                <t t-set="title">Mes objectifs</t>
            </t>

            <div t-if="success_message" class="alert alert-success mt-3" role="alert">
                <t t-esc="success_message"/>
            </div>
            <div t-if="error.get('global')" class="alert alert-danger mt-3" role="alert">
                <t t-esc="error['global']"/>
            </div>

            <p class="text-muted mt-3">
                Voici ce que votre responsable attend de vous. Vous ne
                modifiez pas ces objectifs — vous déclarez où vous en êtes,
                tout au long de l'année.
            </p>

            <t t-foreach="lignes" t-as="ligne">
                <t t-set="periode" t-value="ligne['periode']"/>

                <div class="d-flex justify-content-between align-items-baseline mt-4 mb-2">
                    <h5 class="mb-0">Exercice <t t-esc="periode.exercice"/></h5>
                    <span class="text-muted small" t-esc="periode.name"/>
                </div>

                <form method="post" action="/my/mes-objectifs">
                    <input type="hidden" name="csrf_token"
                           t-att-value="request.csrf_token()"/>
                    <input type="hidden" name="periode_id"
                           t-att-value="periode.id"/>

                    <!-- UN OBJECTIF = UNE CARTE. Ce qu'on lui demande à
                         gauche, où il en est à droite : la seule chose
                         qu'il ait à renseigner tombe toujours au même
                         endroit. -->
                    <div t-foreach="ligne['objectifs']" t-as="obj" class="card mb-3">
                        <div class="card-header">
                            <div class="row g-2 align-items-center">
                                <div class="col-md-8">
                                    <div class="fw-bold"><t t-esc="obj.name"/></div>
                                    <div class="text-muted small">
                                        Fixé par <t t-esc="obj.manager_id.name"/>
                                        <t t-if="obj.deadline">
                                            — échéance <span t-field="obj.deadline"/>
                                        </t>
                                    </div>
                                    <div t-if="obj.description"
                                         class="text-muted small mt-1">
                                        <t t-out="obj.description"/>
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <label class="form-label small mb-1"
                                           t-attf-for="progress_#{obj.id}">
                                        Où j'en suis
                                    </label>
                                    <select t-attf-name="progress_#{obj.id}"
                                            t-attf-id="progress_#{obj.id}"
                                            class="form-select form-select-sm">
                                        <option t-foreach="niveaux" t-as="niv"
                                                t-att-value="niv[0]"
                                                t-att-selected="'selected' if obj.progression == niv[0] else None">
                                            <t t-esc="niv[1]"/>
                                        </option>
                                    </select>
                                </div>
                            </div>
                        </div>

                        <div class="card-body">
                            <t t-if="not obj.ev_activite_ids">
                                <p class="text-muted small mb-0">
                                    Le détail des activités n'a pas encore été
                                    précisé par votre responsable.
                                </p>
                            </t>
                            <t t-else="">
                                <div class="text-muted small mb-2">
                                    Ce qui est attendu de vous
                                </div>
                                <table class="table table-sm align-middle mb-0">
                                    <thead>
                                        <tr class="active">
                                            <th style="width: 45%;">Activité</th>
                                            <th>Résultat attendu</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <tr t-foreach="obj.ev_activite_ids" t-as="act">
                                            <td><t t-esc="act.name"/></td>
                                            <td class="text-muted">
                                                <t t-if="act.resultat_attendu">
                                                    <t t-esc="act.resultat_attendu"/>
                                                </t>
                                                <span t-else="">—</span>
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>
                            </t>
                        </div>
                    </div>

                    <div class="mb-4">
                        <button type="submit" class="btn btn-sm btn-primary">
                            Enregistrer mon avancement
                        </button>
                    </div>
                </form>
            </t>
        </t>
    </template>

    <!-- ============ Objectifs : niveau 1, les exercices ============ -->
    <template id="portal_objectifs_exercices" name="Objectifs — exercices">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>
            <t t-call="portal.portal_searchbar">
                <t t-set="title">Objectifs de mes collaborateurs</t>
            </t>

            <div t-if="success_message" class="alert alert-success mt-3" role="alert">
                <t t-esc="success_message"/>
            </div>

            <p class="text-muted mt-3">
                Les objectifs se fixent en début d'exercice. Choisissez
                l'exercice pour voir vos collaborateurs.
            </p>

            <div class="list-group mt-2">
                <a t-foreach="lignes" t-as="ligne"
                   t-attf-href="/my/objectifs/#{ligne['periode'].id}"
                   class="list-group-item list-group-item-action">
                    <div class="d-flex justify-content-between align-items-start gap-3">
                        <div>
                            <div class="fw-bold">
                                <t t-esc="ligne['periode'].name"/>
                            </div>
                            <div class="text-muted small">
                                Exercice <t t-esc="ligne['periode'].exercice"/> —
                                du <span t-field="ligne['periode'].date_debut"/>
                                au <span t-field="ligne['periode'].date_fin"/>
                            </div>
                        </div>
                        <div class="text-end">
                            <span t-if="ligne['periode'].ouverte"
                                  class="badge text-bg-success">Ouverte</span>
                            <span t-else="" class="badge text-bg-secondary">Fermée</span>
                            <div class="small mt-1">
                                <t t-esc="ligne['agents']"/>
                                collaborateur<t t-if="ligne['agents'] &gt; 1">s</t>
                            </div>
                            <div t-if="ligne['sans_objectif']"
                                 class="small text-danger fw-bold">
                                <t t-esc="ligne['sans_objectif']"/> sans objectif
                            </div>
                        </div>
                    </div>
                </a>
            </div>
        </t>
    </template>

    <!-- ======== Objectifs : niveau 2, mes collaborateurs ======== -->
    <template id="portal_objectifs_collaborateurs" name="Objectifs — collaborateurs">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>
            <t t-call="portal.portal_searchbar">
                <t t-set="title"><t t-esc="periode.name"/></t>
            </t>

            <div t-if="success_message" class="alert alert-success mt-3" role="alert">
                <t t-esc="success_message"/>
            </div>
            <div t-if="motif_ferme" class="alert alert-secondary mt-3" role="alert">
                <t t-esc="motif_ferme"/>
            </div>

            <div class="d-flex justify-content-between align-items-center mt-3">
                <div class="text-muted">
                    Exercice <strong t-esc="periode.exercice"/> —
                    saisie jusqu'au <span t-field="periode.date_fin"/>
                </div>
                <a href="/my/objectifs" class="btn btn-sm btn-light">
                    <i class="fa fa-angle-left"/> Tous les exercices
                </a>
            </div>

            <div class="list-group mt-3">
                <a t-foreach="agents" t-as="bloc"
                   t-attf-href="/my/objectifs/#{periode.id}/#{bloc['employee'].id}"
                   class="list-group-item list-group-item-action">
                    <div class="d-flex justify-content-between align-items-start gap-3">
                        <div>
                            <div class="fw-bold">
                                <t t-esc="bloc['employee'].name"/>
                            </div>
                            <div t-if="bloc['employee'].job_title"
                                 class="text-muted small">
                                <t t-esc="bloc['employee'].job_title"/>
                            </div>
                        </div>
                        <div class="text-end small">
                            <t t-if="not bloc['nb_objectifs']">
                                <span class="badge text-bg-danger">Aucun objectif</span>
                            </t>
                            <t t-else="">
                                <div>
                                    <t t-esc="bloc['nb_objectifs']"/>
                                    objectif<t t-if="bloc['nb_objectifs'] &gt; 1">s</t>,
                                    <t t-esc="bloc['nb_activites']"/>
                                    activité<t t-if="bloc['nb_activites'] &gt; 1">s</t>
                                </div>
                                <div t-if="bloc['objectifs_sans_activite']"
                                     class="text-danger fw-bold">
                                    <t t-esc="bloc['objectifs_sans_activite']"/>
                                    sans activité
                                </div>
                            </t>
                        </div>
                    </div>
                </a>
            </div>
        </t>
    </template>

    <!-- ===== Objectifs : niveau 3, le détail d'un collaborateur ===== -->
    <template id="portal_objectifs_agent" name="Objectifs — collaborateur">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>
            <t t-call="portal.portal_searchbar">
                <t t-set="title"><t t-esc="agent.name"/></t>
            </t>

            <div t-if="success_message" class="alert alert-success mt-3" role="alert">
                <t t-esc="success_message"/>
            </div>
            <div t-if="error.get('global')" class="alert alert-danger mt-3" role="alert">
                <t t-esc="error['global']"/>
            </div>
            <div t-if="motif_ferme" class="alert alert-secondary mt-3" role="alert">
                <t t-esc="motif_ferme"/>
            </div>

            <div class="d-flex justify-content-between align-items-center mt-3 mb-1">
                <div class="text-muted">
                    <t t-if="agent.job_title">
                        <t t-esc="agent.job_title"/> —
                    </t>
                    <t t-esc="periode.name"/>, exercice
                    <strong t-esc="periode.exercice"/>
                </div>
                <a t-attf-href="/my/objectifs/#{periode.id}"
                   class="btn btn-sm btn-light">
                    <i class="fa fa-angle-left"/> Mes collaborateurs
                </a>
            </div>

            <t t-if="not objectifs">
                <div class="alert alert-warning" role="alert">
                    Aucun objectif n'a encore été fixé à
                    <t t-esc="agent.name"/> pour cet exercice.
                </div>
            </t>

            <!-- UN OBJECTIF = UN BLOC. L'en-tête porte l'objectif, le
                 corps ses activités : c'est cette séparation qui manquait
                 quand tout était empilé dans un seul tableau. -->
            <div t-foreach="objectifs" t-as="obj" class="card mb-3">
                <div class="card-header d-flex justify-content-between align-items-start gap-3">
                    <div>
                        <div class="fw-bold"><t t-esc="obj.name"/></div>
                        <div class="text-muted small">
                            <t t-if="obj.deadline">
                                Échéance <span t-field="obj.deadline"/> —
                            </t>
                            Avancement déclaré :
                            <t t-esc="dict(obj._fields['progression'].selection).get(obj.progression, '')"/>
                        </div>
                        <div t-if="obj.description" class="text-muted small mt-1">
                            <t t-out="obj.description"/>
                        </div>
                    </div>
                    <form t-if="saisie_ouverte" method="post"
                          t-attf-action="/my/objectifs/#{periode.id}/#{agent.id}">
                        <input type="hidden" name="csrf_token"
                               t-att-value="request.csrf_token()"/>
                        <input type="hidden" name="action" value="goal_remove"/>
                        <input type="hidden" name="goal_id" t-att-value="obj.id"/>
                        <button type="submit"
                                class="btn btn-sm btn-link text-danger p-0"
                                title="Retirer cet objectif">
                            <i class="fa fa-trash"/>
                        </button>
                    </form>
                </div>

                <div class="card-body">
                    <t t-if="not obj.ev_activite_ids">
                        <div class="alert alert-danger py-2 mb-3" role="alert">
                            Aucune activité : cet objectif ne pourra pas être
                            évalué en fin d'exercice.
                        </div>
                    </t>
                    <table t-else="" class="table table-sm align-middle mb-3">
                        <thead>
                            <tr class="active">
                                <th style="width: 40%;">Activité</th>
                                <th>Résultat attendu</th>
                                <th style="width: 40px;"/>
                            </tr>
                        </thead>
                        <tbody>
                            <tr t-foreach="obj.ev_activite_ids" t-as="act">
                                <td><t t-esc="act.name"/></td>
                                <td>
                                    <t t-if="act.resultat_attendu">
                                        <t t-esc="act.resultat_attendu"/>
                                    </t>
                                    <span t-else="" class="text-warning">
                                        à préciser
                                    </span>
                                </td>
                                <td class="text-end">
                                    <form t-if="saisie_ouverte" method="post"
                                          t-attf-action="/my/objectifs/#{periode.id}/#{agent.id}">
                                        <input type="hidden" name="csrf_token"
                                               t-att-value="request.csrf_token()"/>
                                        <input type="hidden" name="action"
                                               value="activite_remove"/>
                                        <input type="hidden" name="activite_id"
                                               t-att-value="act.id"/>
                                        <button type="submit"
                                                class="btn btn-sm btn-link text-danger p-0"
                                                title="Retirer cette activité">
                                            <i class="fa fa-times"/>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        </tbody>
                    </table>

                    <form t-if="saisie_ouverte" method="post"
                          t-attf-action="/my/objectifs/#{periode.id}/#{agent.id}"
                          class="row g-2">
                        <input type="hidden" name="csrf_token"
                               t-att-value="request.csrf_token()"/>
                        <input type="hidden" name="action" value="activite_add"/>
                        <input type="hidden" name="goal_id" t-att-value="obj.id"/>
                        <div class="col-md-5">
                            <input type="text" name="activite_name" maxlength="300"
                                   required="required" class="form-control form-control-sm"
                                   placeholder="Activité — ce qu'il doit faire"/>
                        </div>
                        <div class="col-md-5">
                            <input type="text" name="resultat_attendu"
                                   class="form-control form-control-sm"
                                   placeholder="Résultat attendu"/>
                        </div>
                        <div class="col-md-2">
                            <button type="submit"
                                    class="btn btn-sm btn-outline-secondary w-100">
                                <i class="fa fa-plus"/> Activité
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <!-- L'ajout d'un objectif, à part : c'est un geste d'une autre
                 nature que la saisie des activités. -->
            <div t-if="saisie_ouverte" class="card mb-4">
                <div class="card-header fw-bold">Ajouter un objectif</div>
                <div class="card-body">
                    <form method="post"
                          t-attf-action="/my/objectifs/#{periode.id}/#{agent.id}"
                          class="row g-2">
                        <input type="hidden" name="csrf_token"
                               t-att-value="request.csrf_token()"/>
                        <input type="hidden" name="action" value="goal_add"/>
                        <div class="col-md-5">
                            <input type="text" name="goal_name" maxlength="200"
                                   required="required" class="form-control form-control-sm"
                                   placeholder="Objectif — une phrase"/>
                        </div>
                        <div class="col-md-3">
                            <input type="text" name="goal_description"
                                   class="form-control form-control-sm"
                                   placeholder="Précisions (facultatif)"/>
                        </div>
                        <div class="col-md-2">
                            <input type="date" name="goal_deadline"
                                   class="form-control form-control-sm"
                                   title="Échéance"/>
                        </div>
                        <div class="col-md-2">
                            <button type="submit" class="btn btn-sm btn-primary w-100">
                                <i class="fa fa-plus"/> Ajouter
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Recopier vers d'autres collaborateurs. Dans une même
                 équipe, plusieurs agents portent souvent la même
                 commande : les ressaisir un par un est long, et les
                 écarts de formulation rendent ensuite les notes
                 incomparables. -->
            <div t-if="saisie_ouverte and objectifs"
                 class="card mb-4">
                <div class="card-header fw-bold">
                    Recopier ces objectifs vers un autre collaborateur
                </div>
                <div class="card-body">
                    <div t-if="not autres_agents" class="alert alert-secondary mb-0"
                         role="alert">
                        <t t-esc="agent.name"/> est votre seul collaborateur
                        visé par cette période : il n'y a personne vers qui
                        recopier. Pour partager ces objectifs avec d'autres
                        agents, demandez au service RH d'élargir la
                        population visée par
                        « <t t-esc="periode.name"/> ».
                    </div>
                    <t t-else="">
                    <p class="text-muted small">
                        Les objectifs de <t t-esc="agent.name"/>, leurs
                        activités et les résultats attendus sont recopiés
                        tels quels. Vous pourrez ensuite les ajuster
                        collaborateur par collaborateur. Un objectif que
                        l'agent porte déjà n'est pas recopié une seconde
                        fois.
                    </p>
                    <form method="post"
                          t-attf-action="/my/objectifs/#{periode.id}/#{agent.id}">
                        <input type="hidden" name="csrf_token"
                               t-att-value="request.csrf_token()"/>
                        <input type="hidden" name="action"
                               value="goals_duplicate"/>
                        <div class="row g-2">
                            <div t-foreach="autres_agents" t-as="ligne"
                                 class="col-md-6">
                                <t t-set="autre" t-value="ligne[0]"/>
                                <t t-set="nb" t-value="ligne[1]"/>
                                <div class="form-check">
                                    <input class="form-check-input"
                                           type="checkbox" name="cible_ids"
                                           t-att-value="autre.id"
                                           t-attf-id="cible_#{autre.id}"/>
                                    <label class="form-check-label"
                                           t-attf-for="cible_#{autre.id}">
                                        <t t-esc="autre.name"/>
                                        <span t-if="nb" class="text-muted small">
                                            — a déjà <t t-esc="nb"/> objectif(s)
                                        </span>
                                        <span t-else="" class="text-muted small">
                                            — aucun objectif
                                        </span>
                                    </label>
                                </div>
                            </div>
                        </div>
                        <div class="mt-3">
                            <button type="submit"
                                    class="btn btn-sm btn-outline-primary">
                                <i class="fa fa-copy"/> Recopier les objectifs
                            </button>
                        </div>
                    </form>
                    </t>
                </div>
            </div>
        </t>
    </template>

    <!-- ==================== Liste de mes évaluations ==================== -->
    <template id="portal_my_appraisals" name="Mes évaluations">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>
            <t t-call="portal.portal_searchbar">
                <t t-set="title">Mes évaluations</t>
            </t>

            <div t-if="success_message" class="alert alert-success mt-3" role="alert">
                <t t-esc="success_message"/>
            </div>

            <t t-if="not appraisals">
                <div class="alert alert-info mt-3" role="alert">
                    Aucune évaluation ne vous concerne pour le moment.
                </div>
            </t>
            <t t-if="appraisals" t-call="portal.portal_table">
                <thead>
                    <tr class="active">
                        <th>Campagne</th>
                        <th>Exercice</th>
                        <th>Entretien prévu</th>
                        <th class="text-center">Statut</th>
                        <th class="text-end">Détail</th>
                    </tr>
                </thead>
                <tbody>
                    <tr t-foreach="appraisals" t-as="ap">
                        <td>
                            <a t-attf-href="/my/evaluations/#{ap.id}">
                                <t t-esc="ap.ev_campagne_id.name or 'Évaluation annuelle'"/>
                            </a>
                        </td>
                        <td><t t-esc="ap.ev_campagne_id.exercice or ''"/></td>
                        <td><span t-field="ap.ev_entretien_debut" t-options="{'format': 'dd/MM/yyyy à HH:mm'}"/></td>
                        <td class="text-center">
                            <span t-if="ap.state == 'done'" class="badge rounded-pill text-bg-success">
                                <t t-esc="state_labels.get(ap.state, ap.state)"/>
                            </span>
                            <span t-elif="ap.state == 'cancel'" class="badge rounded-pill text-bg-danger">
                                <t t-esc="state_labels.get(ap.state, ap.state)"/>
                            </span>
                            <span t-else="" class="badge rounded-pill text-bg-info">
                                <t t-esc="state_labels.get(ap.state, ap.state)"/>
                            </span>
                        </td>
                        <td class="text-end">
                            <a t-attf-href="/my/evaluations/#{ap.id}"
                               class="btn btn-sm btn-primary">
                                <i class="fa fa-eye"/> Voir
                            </a>
                        </td>
                    </tr>
                </tbody>
            </t>
        </t>
    </template>

    <!-- ============ Grille notée, en lecture ============
         Les deux colonnes côte à côte : l'auto-évaluation de l'agent et
         la note du responsable, celle qui fait foi. Le même tableau sert
         à l'agent, au responsable une fois sa notation publiée, et au
         N+2 qui statue — seuls les intitulés de colonnes changent.
         La visibilité de chaque colonne reste décidée par le module
         métier (`ev_voir_notes_agent` / `ev_voir_notes_manager`). -->
    <!-- ===================== La fiche d'évaluation =====================
         Un seul gabarit pour les deux écrans : le responsable la
         remplit, le collaborateur la lit une fois publiée. L'appelant
         pose `modifiable` — rien d'autre ne change d'un écran à
         l'autre, et les deux affichent donc exactement la même note. -->
    <template id="portal_fiche_evaluation" name="Portail : fiche d'évaluation">
        <t t-set="notes"
           t-value="{l.critere_id.id: l for l in appraisal.ev_note_ids}"/>
        <t t-set="groupes"
           t-value="appraisal._ev_mesures_par_objectif()"/>

        <t t-foreach="appraisal._ev_sections()" t-as="sec">
            <t t-set="bloc" t-value="sec['bloc']"/>

            <!-- L'en-tête de section porte son poids et où
                 elle en est : le responsable sait à tout
                 moment ce que cette partie pèse. -->
            <div class="d-flex justify-content-between align-items-baseline border-bottom mt-4 mb-2 pb-1">
                <h6 class="mb-0">
                    <strong t-esc="bloc.name"/>
                    <span class="text-muted ms-2">
                        <t t-esc="'%g' % bloc.poids"/> % de la note
                    </span>
                </h6>
                <span class="text-muted small">
                    <t t-esc="'%.1f' % sec['obtenu']"/>
                    / <t t-esc="'%g' % sec['maximum']"/>
                </span>
            </div>

            <!-- Section OBJECTIFS -->
            <t t-if="bloc.type_section == 'objectifs'">
                <t t-if="not groupes">
                    <div class="alert alert-warning py-2" role="alert">
                        Aucun objectif n'a été fixé à
                        <t t-esc="appraisal.employee_id.name"/>
                        pour cet exercice : cette section
                        vaudra zéro. Demandez au service RH
                        de rouvrir la période de fixation.
                    </div>
                </t>
                <p t-else="" class="text-muted small">
                    Constatez ce qui a été fait et posez un
                    taux : la note se calcule toute seule à
                    partir de la grille de concordance.
                    <t t-if="appraisal.ev_activites_mesurees &lt; appraisal.ev_activites_total">
                        <strong>
                            <t t-esc="appraisal.ev_activites_mesurees"/>
                            sur <t t-esc="appraisal.ev_activites_total"/>
                            activités mesurées
                        </strong>
                        — les autres ne comptent pas dans la note.
                    </t>
                </p>

                <div t-foreach="groupes" t-as="groupe" class="card mb-3">
                    <t t-set="goal" t-value="groupe[0]"/>
                    <t t-set="mesures" t-value="groupe[1]"/>
                    <t t-set="mesurees" t-value="mesures.filtered('mesuree')"/>
                    <div class="card-header d-flex justify-content-between align-items-start gap-3">
                        <div>
                            <strong t-esc="goal.name"/>
                            <div t-if="goal.description"
                                 class="text-muted small"
                                 t-field="goal.description"/>
                            <div class="text-muted small">
                                <t t-if="goal.deadline">
                                    Échéance <span t-field="goal.deadline"/> —
                                </t>
                                Avancement déclaré :
                                <span t-field="goal.progression"/>
                            </div>
                        </div>
                        <div t-if="mesurees" class="text-end fw-bold">
                            <t t-esc="'%.1f' % (sum(mesurees.mapped('note')) / len(mesurees))"/>
                            <span class="text-muted fw-normal">
                                / <t t-esc="'%g' % appraisal.ev_note_objectifs_max"/>
                            </span>
                        </div>
                    </div>
                    <div class="card-body">
                        <t t-if="not mesures">
                            <div class="alert alert-warning py-2 mb-0" role="alert">
                                Aucune activité n'a été définie
                                sous cet objectif : il ne peut
                                pas être noté.
                            </div>
                        </t>
                        <div t-else="" class="table-responsive">
                            <table class="table table-sm align-middle mb-0">
                                <thead>
                                    <tr class="active">
                                        <th style="width: 24%;">Activité</th>
                                        <th style="width: 24%;">Résultat attendu</th>
                                        <th>Résultat atteint</th>
                                        <th style="width: 7rem;">Taux (%)</th>
                                        <th style="width: 5rem;" class="text-center">Note</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <tr t-foreach="mesures" t-as="act">
                                        <td><t t-esc="act.name"/></td>
                                        <td class="text-muted">
                                            <t t-if="act.resultat_attendu">
                                                <t t-esc="act.resultat_attendu"/>
                                            </t>
                                            <span t-else="">—</span>
                                        </td>
                                        <td>
                                            <textarea t-if="modifiable"
                                                      t-attf-name="atteint_#{act.id}"
                                                      class="form-control form-control-sm"
                                                      rows="2"
                                                      placeholder="Ce qui a réellement été fait"><t t-esc="act.resultat_atteint or ''"/></textarea>
                                            <t t-else="">
                                                <t t-if="act.resultat_atteint">
                                                    <t t-esc="act.resultat_atteint"/>
                                                </t>
                                                <span t-else="" class="text-muted">—</span>
                                            </t>
                                        </td>
                                        <td>
                                            <input t-if="modifiable"
                                                   type="number" min="0" max="100" step="0.01"
                                                   t-attf-name="taux_#{act.id}"
                                                   class="form-control form-control-sm text-end"
                                                   t-att-value="('%g' % act.taux_realisation) if act.mesuree else ''"
                                                   placeholder="0"/>
                                            <t t-else="">
                                                <t t-if="act.mesuree">
                                                    <t t-esc="'%g' % act.taux_realisation"/> %
                                                </t>
                                                <span t-else="" class="text-muted">—</span>
                                            </t>
                                        </td>
                                        <td class="text-center fw-bold">
                                            <t t-if="act.mesuree">
                                                <t t-esc="'%g' % act.note"/>
                                            </t>
                                            <span t-else="" class="text-muted">—</span>
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </t>

            <!-- Section QUESTIONS NOTEES -->
            <t t-else="">
                <div class="table-responsive">
                    <table class="table table-sm align-middle">
                        <thead>
                            <tr class="active">
                                <th>Question</th>
                                <th style="width: 9rem;" class="text-end">Note</th>
                            </tr>
                        </thead>
                        <tbody>
                            <t t-foreach="bloc.theme_ids" t-as="theme">
                                <tr t-foreach="theme.critere_ids" t-as="critere">
                                    <t t-set="ligne" t-value="notes.get(critere.id)"/>
                                    <td>
                                        <t t-esc="critere.name"/>
                                        <div t-if="critere.description"
                                             class="text-muted small"
                                             t-esc="critere.description"/>
                                        <div t-if="vue.ev_voir_notes_agent and ligne and ligne.valeur_agent"
                                             class="text-muted small">
                                            Auto-évaluation :
                                            <t t-esc="'%g' % ligne.valeur_agent"/>
                                        </div>
                                    </td>
                                    <td class="text-end">
                                        <div class="d-flex align-items-center justify-content-end gap-2">
                                            <input t-if="ligne and modifiable"
                                                   type="number" min="0"
                                                   t-att-max="critere.points_max"
                                                   step="0.5"
                                                   class="form-control form-control-sm text-end"
                                                   style="max-width: 6rem;"
                                                   t-attf-name="points_#{ligne.id}"
                                                   t-att-value="('%g' % ligne.valeur_manager) if ligne.valeur_manager else ''"
                                                   placeholder="0"/>
                                            <span t-elif="ligne" class="fw-bold">
                                                <t t-esc="'%g' % ligne.valeur_manager"/>
                                            </span>
                                            <span class="text-muted">
                                                / <t t-esc="'%g' % critere.points_max"/>
                                            </span>
                                        </div>
                                    </td>
                                </tr>
                            </t>
                        </tbody>
                    </table>
                </div>
            </t>
        </t>

        <!-- Le recapitulatif -->
        <h6 class="mt-4">Récapitulatif</h6>
        <div class="table-responsive">
            <table class="table table-sm align-middle"
                   style="max-width: 46rem;">
                <thead>
                    <tr class="active">
                        <th>Section</th>
                        <th class="text-end">Obtenu</th>
                        <th class="text-end">Sur 100</th>
                        <th class="text-end">Poids</th>
                        <th class="text-end">Apport</th>
                    </tr>
                </thead>
                <tbody>
                    <tr t-foreach="appraisal._ev_sections()" t-as="sec">
                        <td><t t-esc="sec['bloc'].name"/></td>
                        <td class="text-end">
                            <t t-esc="'%.1f' % sec['obtenu']"/>
                            / <t t-esc="'%g' % sec['maximum']"/>
                        </td>
                        <td class="text-end">
                            <t t-esc="'%.1f' % sec['pourcentage']"/> %
                        </td>
                        <td class="text-end text-muted">
                            <t t-esc="'%g' % sec['poids']"/> %
                        </td>
                        <td class="text-end fw-bold">
                            <t t-esc="'%.1f' % sec['apport']"/>
                        </td>
                    </tr>
                    <tr class="table-active">
                        <td class="fw-bold" colspan="4">Note globale</td>
                        <td class="text-end fw-bold">
                            <t t-esc="'%.1f' % appraisal.ev_note_finale"/>
                            / <t t-esc="'%g' % appraisal.ev_note_finale_max"/>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
        <p class="text-muted small">
            Chaque section donne un score sur 100 ; la note
            finale est la somme de leurs apports, ramenée sur
            <t t-esc="'%g' % appraisal.ev_note_finale_max"/> —
            soit <t t-esc="'%.1f' % appraisal.ev_note_finale_sur10"/> / 10.
            Les totaux se mettent à jour après enregistrement.
        </p>
    </template>

    <!-- ===================== Détail d'une évaluation ===================== -->
    <template id="portal_my_appraisal_detail" name="Mon évaluation">
        <t t-call="portal.portal_layout">
            <div class="row mt-3">
                <div class="col-12 col-lg-9">
                    <h4>
                        <t t-esc="appraisal.ev_campagne_id.name or 'Évaluation annuelle'"/>
                        <span t-if="appraisal.state == 'done'" class="badge rounded-pill text-bg-success ms-2">
                            <t t-esc="state_labels.get(appraisal.state)"/>
                        </span>
                        <span t-else="" class="badge rounded-pill text-bg-info ms-2">
                            <t t-esc="state_labels.get(appraisal.state)"/>
                        </span>
                    </h4>

                    <div t-if="success_message" class="alert alert-success" role="alert">
                        <t t-esc="success_message"/>
                    </div>
                    <div t-if="error and error.get('global')"
                         class="alert alert-danger" role="alert">
                        <t t-esc="error['global']"/>
                    </div>

                    <div t-if="appraisal.ev_motif_renvoi"
                         class="alert alert-warning" role="alert">
                        <strong><i class="fa fa-undo"/> Renvoyé pour correction<t t-if="appraisal.ev_renvoi_par"> par <t t-esc="appraisal.ev_renvoi_par"/></t>.</strong>
                        <div class="mt-1" style="white-space: pre-line;"><t t-esc="appraisal.ev_motif_renvoi"/></div>
                    </div>

                    <!-- Invitation à s'auto-évaluer -->
                    <div t-if="afficher_grille and vue.ev_auto_modifiable" class="alert alert-info" role="alert">
                        <strong>C'est à vous.</strong>
                        Remplissez votre auto-évaluation sur la grille de la
                        campagne, puis publiez-la : votre responsable ne la
                        découvrira qu'à ce moment.
                        <div class="mt-2">
                            <a t-attf-href="/my/evaluations/#{appraisal.id}/auto-evaluation"
                               class="btn btn-sm btn-primary">
                                <i class="fa fa-pencil"/> Remplir mon auto-évaluation
                            </a>
                        </div>
                    </div>
                    <div t-if="appraisal.employee_feedback_published"
                         class="alert alert-success" role="alert">
                        <i class="fa fa-check"/> Votre auto-évaluation est publiée.
                    </div>
                    <!-- Auto-évaluation désactivée : mieux vaut le dire que
                         laisser l'agent chercher un formulaire absent. -->
                    <div t-if="not appraisal.ev_auto_evaluation_active"
                         class="alert alert-secondary" role="alert">
                        Cette campagne ne prévoit pas d'auto-évaluation :
                        votre responsable vous notera, et vous verrez sa
                        notation dès qu'il l'aura publiée.
                    </div>

                    <!-- Convocation à l'entretien : c'est une information
                         que l'agent doit voir en arrivant, pas une ligne
                         perdue dans un tableau. -->
                    <div t-if="appraisal.ev_entretien_debut and appraisal.state != 'cancel'"
                         t-attf-class="alert alert-#{'secondary' if appraisal.state == 'done' else 'primary'}"
                         role="alert">
                        <i class="fa fa-calendar"/>
                        <t t-if="appraisal.state == 'done'">
                            <strong>Votre entretien annuel a eu lieu le
                            <span t-field="appraisal.ev_entretien_debut" t-options="{'format': 'dd/MM/yyyy à HH:mm'}"/>.</strong>
                        </t>
                        <t t-else="">
                            <strong>Votre entretien annuel est fixé au
                            <span t-field="appraisal.ev_entretien_debut" t-options="{'format': 'dd/MM/yyyy à HH:mm'}"/>.</strong>
                            <t t-if="appraisal.ev_entretien_avec">
                                Il sera mené par <t t-esc="appraisal.ev_entretien_avec"/>.
                            </t>
                            <div class="mt-1">
                                Vous avez également reçu une invitation dans
                                votre agenda.
                            </div>
                        </t>
                    </div>

                    <h5 class="mt-3">Informations</h5>
                    <table class="table table-sm">
                        <tbody>
                            <tr t-if="appraisal.ev_campagne_id">
                                <th class="w-25">Campagne</th>
                                <td><t t-esc="appraisal.ev_campagne_id.name"/>
                                    <t t-if="appraisal.ev_campagne_id.exercice">
                                        — exercice <t t-esc="appraisal.ev_campagne_id.exercice"/>
                                    </t>
                                </td>
                            </tr>
                            <tr t-if="appraisal.manager_ids">
                                <th>Évaluateur(s)</th>
                                <td><t t-esc="', '.join(appraisal.manager_ids.mapped('name'))"/></td>
                            </tr>
                            <tr t-if="appraisal.ev_phase_courante_id">
                                <th>Phase en cours</th>
                                <td><t t-esc="appraisal.ev_phase_courante_id.name"/></td>
                            </tr>
                            <tr t-if="appraisal.ev_entretien_debut">
                                <th>Entretien prévu</th>
                                <td><span t-field="appraisal.ev_entretien_debut" t-options="{'format': 'dd/MM/yyyy à HH:mm'}"/></td>
                            </tr>
                        </tbody>
                    </table>

                    <!-- Les objectifs de l'exercice : la commande passée à
                         l'agent. Il ne les écrit pas — c'est son
                         responsable qui les fixe — mais c'est LUI qui
                         déclare où il en est, tout au long de l'année. -->
                    <h5 class="mt-4">Mes objectifs de l'exercice</h5>
                    <t t-if="not appraisal.ev_objectif_ids">
                        <div class="alert alert-info" role="alert">
                            Aucun objectif n'a encore été fixé pour cet
                            exercice.
                        </div>
                    </t>
                    <form t-if="appraisal.ev_objectif_ids" method="post"
                          t-attf-action="/my/evaluations/#{appraisal.id}">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <div class="table-responsive">
                            <table class="table table-sm align-middle">
                                <thead>
                                    <tr class="active">
                                        <th>Objectif</th>
                                        <th style="width: 8rem;">Échéance</th>
                                        <th style="width: 9rem;">Avancement</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <tr t-foreach="appraisal.ev_objectif_ids" t-as="goal">
                                        <td>
                                            <strong t-esc="goal.name"/>
                                            <div t-if="goal.description"
                                                 class="text-muted small"
                                                 t-field="goal.description"/>
                                        </td>
                                        <td>
                                            <span t-if="goal.deadline" t-field="goal.deadline"/>
                                            <span t-else="" class="text-muted">—</span>
                                        </td>
                                        <td>
                                            <select t-if="vue.ev_avancement_modifiable"
                                                    class="form-select form-select-sm"
                                                    t-attf-name="progress_#{goal.id}">
                                                <option t-foreach="progressions or []" t-as="prog"
                                                        t-att-value="prog[0]"
                                                        t-att-selected="'selected' if goal.progression == prog[0] else None">
                                                    <t t-esc="prog[1]"/>
                                                </option>
                                            </select>
                                            <span t-else="" t-field="goal.progression"/>
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                        <div t-if="vue.ev_avancement_modifiable" class="mb-3">
                            <button type="submit" name="action" value="progress_save"
                                    class="btn btn-sm btn-primary">
                                <i class="fa fa-save"/> Enregistrer l'avancement
                            </button>
                            <span class="text-muted small ms-2">
                                Mettez-le à jour au fil de l'année : votre
                                responsable le voit.
                            </span>
                        </div>
                        <p t-if="not vue.ev_avancement_modifiable and motif_avancement"
                           class="text-muted small">
                            <i class="fa fa-lock"/> <t t-esc="motif_avancement"/>
                        </p>
                    </form>

                    <!-- La fiche telle que le responsable l'a remplie.
                         Tant qu'il ne l'a pas publiée, on le dit : une
                         fiche vide sans un mot d'explication se lit
                         comme une note de zéro. -->
                    <t t-if="appraisal.ev_grille_id">
                        <h5 class="mt-4">
                            Ma fiche d'évaluation
                            <span class="text-muted">
                                — <t t-esc="appraisal.ev_grille_id.name"/>
                            </span>
                        </h5>
                        <t t-if="vue.ev_voir_notes_manager">
                            <t t-call="portail_evaluation.portal_fiche_evaluation">
                                <t t-set="modifiable" t-value="False"/>
                            </t>
                        </t>
                        <div t-else="" class="alert alert-info" role="alert">
                            Votre responsable n'a pas encore publié sa
                            notation. Vous verrez ici votre fiche complète —
                            objectifs, questions et note globale — dès qu'il
                            l'aura fait.
                        </div>
                    </t>

                    <t t-if="vue.ev_voir_notes_agent and appraisal.employee_feedback">
                        <h5 class="mt-4">Mes commentaires</h5>
                        <div t-field="appraisal.employee_feedback"/>
                    </t>
                    <t t-if="vue.ev_voir_notes_manager and appraisal.manager_feedback">
                        <h5 class="mt-4">Commentaires de mon responsable</h5>
                        <div t-field="appraisal.manager_feedback"/>
                    </t>

                    <a href="/my/evaluations" class="btn btn-sm btn-secondary mt-3">
                        <i class="fa fa-arrow-left"/> Retour à mes évaluations
                    </a>
                </div>
            </div>
        </t>
    </template>

    <!-- ================= Formulaire d'auto-évaluation ================= -->
    <template id="portal_my_appraisal_auto" name="Mon auto-évaluation">
        <t t-call="portal.portal_layout">
            <div class="row mt-3">
                <div class="col-12 col-lg-9">
                    <h4>Mon auto-évaluation</h4>
                    <p class="text-muted">
                        Positionnez-vous sur chaque critère. Vous pouvez
                        enregistrer et revenir plus tard ; une fois publiée,
                        votre auto-évaluation n'est plus modifiable et devient
                        visible de votre responsable.
                    </p>

                    <div t-if="error.get('global')" class="alert alert-danger" role="alert">
                        <t t-esc="error['global']"/>
                    </div>
                    <div t-if="appraisal.ev_motif_renvoi"
                         class="alert alert-warning" role="alert">
                        <strong><i class="fa fa-undo"/> Renvoyé pour correction<t t-if="appraisal.ev_renvoi_par"> par <t t-esc="appraisal.ev_renvoi_par"/></t>.</strong>
                        <div class="mt-1" style="white-space: pre-line;"><t t-esc="appraisal.ev_motif_renvoi"/></div>
                    </div>

                    <div t-if="success_message" class="alert alert-success" role="alert">
                        <t t-esc="success_message"/>
                    </div>

                    <form method="post"
                          t-attf-action="/my/evaluations/#{appraisal.id}/auto-evaluation">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

                        <t t-foreach="appraisal.ev_grille_id.bloc_ids" t-as="bloc">
                            <h5 class="mt-4"><t t-esc="bloc.name"/></h5>
                            <t t-foreach="bloc.theme_ids" t-as="theme">
                                <h6 class="text-muted mt-3"><t t-esc="theme.name"/></h6>
                                <t t-set="notes"
                                   t-value="{l.critere_id.id: l for l in appraisal.ev_note_ids}"/>
                                <div class="mb-3" t-foreach="theme.critere_ids" t-as="critere">
                                    <t t-set="ligne" t-value="notes.get(critere.id)"/>
                                    <label class="form-label" t-attf-for="critere_#{ligne.id if ligne else 0}">
                                        <t t-esc="critere.name"/>
                                        <small t-if="critere.description" class="text-muted">
                                            — <t t-esc="critere.description"/>
                                        </small>
                                    </label>
                                    <select t-if="ligne" class="form-select"
                                            t-attf-name="critere_#{ligne.id}"
                                            t-attf-id="critere_#{ligne.id}">
                                        <option value="">— Non renseigné —</option>
                                        <option t-foreach="niveaux" t-as="niveau"
                                                t-att-value="niveau.id"
                                                t-att-selected="'selected' if ligne.niveau_agent_id.id == niveau.id else None">
                                            <t t-esc="niveau.name"/>
                                        </option>
                                    </select>
                                </div>
                            </t>
                        </t>

                        <h5 class="mt-4">Mes commentaires</h5>
                        <textarea name="employee_feedback" class="form-control" rows="6"
                                  placeholder="Votre appréciation de l'année : réussites, difficultés, attentes..."><t t-esc="employee_feedback_texte"/></textarea>

                        <div class="mt-3">
                            <button type="submit" name="action" value="save"
                                    class="btn btn-secondary">
                                <i class="fa fa-save"/> Enregistrer
                            </button>
                            <button type="submit" name="action" value="publish"
                                    class="btn btn-primary"
                                    onclick="return confirm('Publier votre auto-évaluation ? Votre responsable pourra la consulter, et vous ne pourrez plus la modifier.');">
                                <i class="fa fa-paper-plane"/> Publier
                            </button>
                            <a t-attf-href="/my/evaluations/#{appraisal.id}"
                               class="btn btn-link">Annuler</a>
                        </div>
                    </form>
                </div>
            </div>
        </t>
    </template>

    <!-- ============== Espace évaluateur : liste ============== -->
    <template id="portal_team_appraisals" name="Évaluations de mon équipe">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>
            <t t-call="portal.portal_searchbar">
                <t t-set="title">Évaluations de mon équipe</t>
            </t>

            <div t-if="success_message" class="alert alert-success mt-3" role="alert">
                <t t-esc="success_message"/>
            </div>

            <h5 class="mt-3">En attente de mon action</h5>
            <t t-if="not a_traiter">
                <div class="alert alert-info" role="alert">
                    Aucune évaluation n'attend votre action.
                </div>
            </t>
            <div t-if="a_traiter" class="table-responsive">
                <table class="table table-sm table-striped">
                    <thead>
                        <tr class="active">
                            <th>Collaborateur</th>
                            <th>Campagne</th>
                            <th>Phase en cours</th>
                            <th class="text-end">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr t-foreach="a_traiter" t-as="ap">
                            <td><t t-esc="ap.employee_id.name"/></td>
                            <td><t t-esc="ap.ev_campagne_id.name or 'Évaluation annuelle'"/></td>
                            <td><t t-esc="ap.ev_phase_courante_id.name or ''"/></td>
                            <td class="text-end">
                                <a t-attf-href="/my/team/evaluations/#{ap.id}"
                                   class="btn btn-sm btn-primary">
                                    <i class="fa fa-pencil"/> Traiter
                                </a>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <h5 class="mt-4">Mon périmètre</h5>
            <t t-if="not autres">
                <div class="alert alert-info" role="alert">
                    Aucune autre évaluation dans votre périmètre.
                </div>
            </t>
            <t t-if="autres" t-call="portal.portal_table">
                <thead>
                    <tr class="active">
                        <th>Collaborateur</th>
                        <th>Campagne</th>
                        <th>Phase</th>
                        <th class="text-center">Statut</th>
                        <th class="text-end">Détail</th>
                    </tr>
                </thead>
                <tbody>
                    <tr t-foreach="autres" t-as="ap">
                        <td><t t-esc="ap.employee_id.name"/></td>
                        <td><t t-esc="ap.ev_campagne_id.name or 'Évaluation annuelle'"/></td>
                        <td><t t-esc="ap.ev_phase_courante_id.name or '—'"/></td>
                        <td class="text-center">
                            <span t-if="ap.state == 'done'" class="badge rounded-pill text-bg-success">
                                <t t-esc="state_labels.get(ap.state, ap.state)"/>
                            </span>
                            <span t-else="" class="badge rounded-pill text-bg-info">
                                <t t-esc="state_labels.get(ap.state, ap.state)"/>
                            </span>
                        </td>
                        <td class="text-end">
                            <a t-attf-href="/my/team/evaluations/#{ap.id}"
                               class="btn btn-sm btn-secondary">
                                <i class="fa fa-eye"/> Voir
                            </a>
                        </td>
                    </tr>
                </tbody>
            </t>
        </t>
    </template>

    <!-- ========= Espace évaluateur : noter un collaborateur ========= -->
    <template id="portal_team_appraisal_detail" name="Évaluer un collaborateur">
        <t t-call="portal.portal_layout">
            <div class="row mt-3">
                <div class="col-12 col-lg-9">
                    <h4>
                        Évaluation de <t t-esc="appraisal.employee_id.name"/>
                        <span class="badge rounded-pill text-bg-info ms-2">
                            <t t-esc="state_labels.get(appraisal.state)"/>
                        </span>
                    </h4>
                    <p class="text-muted">
                        <t t-esc="appraisal.ev_campagne_id.name or 'Évaluation annuelle'"/>
                        <t t-if="appraisal.ev_phase_courante_id">
                            — phase : <t t-esc="appraisal.ev_phase_courante_id.name"/>
                        </t>
                    </p>

                    <div t-if="error.get('global')" class="alert alert-danger" role="alert">
                        <t t-esc="error['global']"/>
                    </div>
                    <div t-if="appraisal.ev_motif_renvoi"
                         class="alert alert-warning" role="alert">
                        <strong><i class="fa fa-undo"/> Renvoyé pour correction<t t-if="appraisal.ev_renvoi_par"> par <t t-esc="appraisal.ev_renvoi_par"/></t>.</strong>
                        <div class="mt-1" style="white-space: pre-line;"><t t-esc="appraisal.ev_motif_renvoi"/></div>
                    </div>
                    <div t-if="success_message" class="alert alert-success" role="alert">
                        <t t-esc="success_message"/>
                    </div>
                    <div t-if="appraisal.manager_feedback_published"
                         class="alert alert-success" role="alert">
                        <i class="fa fa-check"/> Votre notation est publiée.
                    </div>
                    <!-- Saisie par anticipation : la grille lui est ouverte
                         avant que le circuit ne lui donne la main. -->
                    <div t-if="vue.ev_notation_modifiable and not vue.ev_can_agir"
                         class="alert alert-info" role="alert">
                        <strong>Vous pouvez noter dès maintenant.</strong>
                        Votre notation ne dépend pas de l'auto-évaluation du
                        collaborateur : remplissez la grille et publiez-la
                        quand vous voulez.
                        <t t-if="appraisal.ev_phase_courante_id">
                            La phase « <t t-esc="appraisal.ev_phase_courante_id.name"/> »
                            suit son cours de son côté ; vous validerez la
                            vôtre quand elle vous reviendra.
                        </t>
                    </div>
                    <div t-if="appraisal.ev_auto_evaluation_active and not appraisal.employee_feedback_published"
                         class="alert alert-secondary" role="alert">
                        L'auto-évaluation du collaborateur ne s'affichera
                        qu'une fois qu'il l'aura publiée.
                    </div>
                    <div t-if="not appraisal.ev_auto_evaluation_active"
                         class="alert alert-secondary" role="alert">
                        Cette campagne ne prévoit pas d'auto-évaluation :
                        votre notation est la seule.
                    </div>

                    <t t-if="vue.ev_voir_notes_agent and appraisal.employee_feedback">
                        <h5 class="mt-3">Commentaires du collaborateur</h5>
                        <div t-field="appraisal.employee_feedback"/>
                    </t>

                    <!-- ================= LA FICHE =================
                         Une seule fiche, un seul enregistrement : les
                         sections dans l'ordre, chacune avec son poids.
                         Deux formulaires séparés faisaient perdre la
                         saisie de l'un quand on enregistrait l'autre. -->
                    <h5 class="mt-4">
                        Fiche d'évaluation
                        <span t-if="appraisal.ev_grille_id" class="text-muted">
                            — <t t-esc="appraisal.ev_grille_id.name"/>
                        </span>
                    </h5>

                    <t t-if="not appraisal.ev_grille_id">
                        <div class="alert alert-warning" role="alert">
                            Aucune fiche n'est rattachée à cette campagne.
                        </div>
                    </t>

                    <form t-if="appraisal.ev_grille_id" method="post"
                          enctype="multipart/form-data"
                          t-attf-action="/my/team/evaluations/#{appraisal.id}">
                        <input type="hidden" name="csrf_token"
                               t-att-value="request.csrf_token()"/>
                        <t t-call="portail_evaluation.portal_fiche_evaluation">
                            <t t-set="modifiable"
                               t-value="vue.ev_notation_modifiable"/>
                        </t>

                            <h5 class="mt-4">Mes commentaires</h5>
                            <textarea name="manager_feedback" class="form-control" rows="6"
                                      t-att-disabled="None if vue.ev_notation_modifiable else 'disabled'"
                                      placeholder="Appréciation générale, points forts, axes de progrès..."><t t-esc="manager_feedback_texte"/></textarea>

                            <div class="mt-3" t-if="vue.ev_notation_modifiable">
                                <button type="submit" name="action" value="save"
                                        class="btn btn-secondary">
                                    <i class="fa fa-save"/> Enregistrer
                                </button>
                                <button type="submit" name="action" value="publish"
                                        class="btn btn-primary"
                                        onclick="return confirm('Publier votre notation ? Le collaborateur pourra la consulter, et vous ne pourrez plus la modifier.');">
                                    <i class="fa fa-paper-plane"/> Publier ma notation
                                </button>
                            </div>

                        <!-- Dernier temps : la note est arrêtée, il reste
                             à la restituer de vive voix. -->
                        <t t-if="vue.ev_entretien_modifiable">
                            <h5 class="mt-4">Entretien annuel</h5>
                            <p class="text-muted">
                                Fixez la date de l'entretien, puis déclarez-le
                                réalisé une fois qu'il aura eu lieu — ce qui
                                clôturera l'évaluation.
                            </p>
                            <t t-set="rdv" t-value="appraisal.meeting_ids[:1]"/>
                            <div class="row">
                                <div class="col-12 col-md-4 mb-2">
                                    <label class="form-label" for="ev_date_entretien">
                                        Date de l'entretien
                                    </label>
                                    <input type="date" name="date_entretien"
                                           id="ev_date_entretien" class="form-control"
                                           t-att-value="rdv.start.strftime('%Y-%m-%d') if rdv else ''"/>
                                </div>
                                <div class="col-12 col-md-3 mb-2">
                                    <label class="form-label" for="ev_heure_entretien">
                                        Heure
                                    </label>
                                    <input type="time" name="heure_entretien"
                                           id="ev_heure_entretien" class="form-control"
                                           t-att-value="rdv.start.strftime('%H:%M') if rdv else '09:00'"/>
                                </div>
                            </div>
                            <p t-if="rdv" class="text-muted small">
                                <i class="fa fa-calendar-check-o"/>
                                Entretien programmé le
                                <strong t-esc="rdv.start.strftime('%d/%m/%Y à %H:%M')"/>.
                                Le collaborateur et vous y êtes conviés.
                            </p>
                            <button type="submit" name="action" value="interview_set"
                                    class="btn btn-secondary btn-sm mb-2">
                                <i class="fa fa-calendar"/> Enregistrer la date
                            </button>

                            <!-- L'entretien se tient hors du logiciel : ce
                                 document en est la seule trace, et il est
                                 exigé avant la clôture. -->
                            <h6 class="mt-3">Compte-rendu de l'entretien</h6>
                            <div t-if="appraisal.ev_compte_rendu"
                                 class="alert alert-success py-2" role="alert">
                                <i class="fa fa-paperclip"/>
                                Déposé :
                                <strong t-esc="appraisal.ev_compte_rendu_nom"/>.
                                Déposer un autre fichier le remplacera.
                            </div>
                            <div t-else="" class="alert alert-warning py-2" role="alert">
                                <i class="fa fa-exclamation-triangle"/>
                                Aucun compte-rendu déposé. Il est exigé avant
                                de pouvoir clôturer l'évaluation.
                            </div>
                            <div class="row">
                                <div class="col-12 col-md-5 mb-2">
                                    <input type="file" name="compte_rendu"
                                           class="form-control form-control-sm"
                                           accept=".pdf,.doc,.docx,.jpg,.jpeg,.png"/>
                                    <span class="text-muted small">
                                        PDF, Word ou image, 5 Mo au maximum.
                                    </span>
                                </div>
                                <div class="col-12 col-md-4 mb-2">
                                    <button type="submit" name="action" value="report_add"
                                            class="btn btn-secondary btn-sm">
                                        <i class="fa fa-upload"/> Déposer le compte-rendu
                                    </button>
                                </div>
                            </div>
                        </t>

                        <t t-if="vue.ev_can_agir">
                            <h5 class="mt-4">Suite du circuit</h5>
                            <div class="mb-2">
                                <label class="form-label" for="ev_comment">
                                    Commentaire
                                    <span class="text-muted small">(obligatoire pour renvoyer)</span>
                                </label>
                                <textarea name="comment" id="ev_comment"
                                          class="form-control" rows="2"/>
                            </div>
                            <button t-if="vue.ev_entretien_modifiable"
                                    type="submit" name="action" value="validate"
                                    class="btn btn-success"
                                    onclick="return confirm('Déclarer l’entretien réalisé ? L’évaluation sera clôturée.');">
                                <i class="fa fa-check"/> L'entretien a eu lieu
                            </button>
                            <button t-else="" type="submit" name="action" value="validate"
                                    class="btn btn-success"
                                    onclick="return confirm('Valider cette phase ?');">
                                <i class="fa fa-check"/> Valider la phase
                            </button>
                            <button type="submit" name="action" value="send_back"
                                    class="btn btn-outline-secondary"
                                    onclick="return confirm('Renvoyer pour correction ?');">
                                <i class="fa fa-undo"/> Renvoyer pour correction
                            </button>
                        </t>
                    </form>

                    <a href="/my/team/evaluations" class="btn btn-sm btn-secondary mt-3">
                        <i class="fa fa-arrow-left"/> Retour aux évaluations à mener
                    </a>
                </div>
            </div>
        </t>
    </template>

</odoo>

<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Section « Évaluations » dans Paramètres généraux.
         Odoo 17 n'a pas d'élément <settings> : on entre par le formulaire
         de base_setup et on insère un <app>. -->
    <record id="res_config_settings_view_form_ev" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.gestion.evaluation</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="62"/>
        <field name="inherit_id" ref="base_setup.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Évaluations" string="Évaluations"
                     name="gestion_evaluation">
                    <block title="Déroulement de l'évaluation" name="ev_block">
                        <setting string="L'agent s'auto-évalue"
                                 help="Décoché, il n'y a plus d'auto-évaluation : l'agent ne voit aucune grille à remplir, et seule la notation de son responsable compte. Le réglage est lu au lancement de chaque campagne — les campagnes déjà ouvertes ne changent pas.">
                            <field name="ev_auto_evaluation_active"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

</odoo>
